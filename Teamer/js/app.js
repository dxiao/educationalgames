// Generated by CoffeeScript 1.7.1
(function() {
  var PlayerAuth;

  PlayerAuth = (function() {
    PlayerAuth.$inject = ['$http'];

    function PlayerAuth($http) {
      this.http = $http;
    }

    PlayerAuth.prototype.submitLogin = function(name, callback) {
      return this.http({
        method: 'GET',
        url: '/teamerapi/login',
        params: {
          name: name
        }
      });
    };

    return PlayerAuth;

  })();

  angular.module('teamer', ['ngRoute']).config(function($routeProvider, $locationProvider) {
    return $routeProvider.when('/login', {
      templateUrl: 'teamer/views/login.html',
      controller: 'LoginController'
    }).otherwise({
      templateUrl: 'teamer/views/null.html'
    });
  }).controller('LoginController', [
    '$scope', 'playerAuth', function($scope, playerAuth) {
      $scope.user = 'blah';
      return $scope.submitName = function() {
        return $scope.id = playerAuth.submitLogin(name).success(function(data, status) {
          return console.log("Success! " + data);
        }).error(function(data, status) {
          return console.log("Error! " + data);
        });
      };
    }
  ]).service('playerAuth', PlayerAuth);

}).call(this);

//# sourceMappingURL=app.map
